interface:
  display_name: "Reviewer"
  short_description: "Review code and test evidence without running tests"
  default_prompt: "Use $reviewer to inspect code and TESTING.md without executing tests, then write REVIEW.md."
policy:
  allow_implicit_invocation: false

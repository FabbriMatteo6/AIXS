# Benchmarks

Shared benchmark definitions belong here when they are used by more than one experiment.

- `workloads/` — prompt/task/context and generation definitions;
- `quality/` — correctness, behavioral and capability checks.

AIXS should avoid inventing a benchmark merely to produce a favorable number. Prefer established evaluations where they answer the question, and document custom workloads when local-system behavior requires them.

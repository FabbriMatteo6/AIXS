# Quality checks

Quality measurement exists to prevent systems optimizations from silently destroying the model behavior we are trying to preserve.

Document what a check measures, its limitations, dataset/version, scoring method and any non-determinism.

# Upstream patches

Store focused, reviewable patches under `patches/<project>/` when an AIXS experiment requires modifications to an external project.

Each patch set should document:

- upstream repository URL;
- exact base commit/revision;
- purpose;
- associated experiment IDs;
- whether the patch has been proposed upstream.

Do not vendor large upstream trees by default.
